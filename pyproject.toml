[project]
name = "my-claude-ai-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "anyio>=4.12.1",
    "claude-agent-sdk>=0.1.50",
]
